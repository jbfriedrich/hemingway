<article class="row text-dark pb-5 {{post_class}}">
    <div class="post-meta col-12 col-md-2 offset-md-1 order-2 order-md-1 pt-md-2 d-flex flex-row flex-md-column justify-content-between justify-content-md-start align-items-baseline align-items-md-center text-muted mr-md-3 mr-0 text-center">
        {{!-- {{#primary_tag}} --}}
        <h5 class="mb-md-4 d-md-flex flex-md-column align-items-md-center justify-content-center meta-tag-list">
            {{#foreach tags}}
                <a class="text-capitalize badge badge-primary mb-md-1 flex-shrink-1 flex-grow-0 post-meta-tag" title="{{description}}" href="{{url}}">{{name}}</a>
            {{/foreach}}
        </h5>
        {{!-- {{/primary_tag}} --}}
        <div class="d-none d-md-block text-capitalize small post-meta-readtime">{{reading_time}}</div>
        <div class="post-meta-date small"><a class="text-muted" name="Date" href="{{url}}">{{date format="DD-MMM-YYYY"}}</a></div>
    </div>
    <div class="col-12 col-md-8 order-1 order-md-2 text-dark post-content{{#has tag="#video,#link,#micro"}} text-justify dynamic-font{{/has}}">
        {{#has tag="#video"}}
            {{content}}
        {{else}}
            {{#has tag="#link"}}
                {{content}}
            {{else}}
                {{#has tag="#micro"}}
                    {{content}}
                {{else}}
                    {{#if feature_image}}
                    {{!-- <figure class="feature-image"> --}}
                        <div class="feature-image" style="background-image: url('{{feature_image}}');"></div>
                    {{!-- </figure> --}}
                    {{/if}}
                    <h2 class="text-left post-title"><a class="stretched-link" name="Blog post: {{title}}" href="{{url}}">{{title}}</a></h2>
                    <p class="post-excerpt text-justify dynamic-font">{{excerpt words=50}}...</p>
                {{/has}}
            {{/has}}
        {{/has}}
    </div>
</article>
